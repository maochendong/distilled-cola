services:
  app:
    build: .
    container_name: distilled-cola
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./data/embeddings:/app/data/embeddings
      - ./.env:/app/.env:ro
    environment:
      - PYTHONUNBUFFERED=1
